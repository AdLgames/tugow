Always the first element on a game screen; it carries the two numbers that matter — the threshold and the boxes left.

```jsx
<HeaderBar floor={5} score={265} threshold={385} boxesLeft={9} logo="assets/logo.svg" />
```

`boxesLeft` of 4 or fewer tints red without any other change. Do not add a health bar; the scorecard is the health bar.
