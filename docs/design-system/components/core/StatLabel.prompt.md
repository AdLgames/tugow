A single dim stat in the header strip. Numbers are always monospaced and tabular.

```jsx
<StatLabel>Floor 5</StatLabel>
<StatLabel>265 / 385</StatLabel>
<StatLabel>9 boxes left</StatLabel>
```

Pass `tone` when the stat turns urgent (e.g. `var(--tb-adversary)` for boxes claimed).
